<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@android:color/white"
    android:layout_marginBottom="8dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- Header: product name + user -->
    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/tvFbProductName"
            android:textStyle="bold"
            android:textSize="15sp"
            android:textColor="#222"
            android:layout_weight="1"
            android:layout_width="0dp"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/tvFbUserName"
            android:textSize="13sp"
            android:textColor="#666"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
    </LinearLayout>

    <!-- Rating -->
    <RatingBar
        android:id="@+id/rbFbRating"
        style="@style/Widget.AppCompat.RatingBar.Small"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:isIndicator="true"
        android:numStars="5"
        android:stepSize="1"
        android:layout_marginTop="4dp" />

    <!-- Comment -->
    <TextView
        android:id="@+id/tvFbComment"
        android:textSize="14sp"
        android:textColor="#444"
        android:layout_marginTop="4dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <!-- Date -->
    <TextView
        android:id="@+id/tvFbDate"
        android:textSize="12sp"
        android:textColor="#999"
        android:layout_marginTop="2dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <View
        android:background="#EEEEEE"
        android:layout_marginVertical="8dp"
        android:layout_width="match_parent"
        android:layout_height="1dp" />

    <!-- Admin reply section -->
    <TextView
        android:text="Phản hồi của admin:"
        android:textStyle="bold"
        android:textSize="13sp"
        android:textColor="#1976D2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <EditText
        android:id="@+id/edtAdminReply"
        android:hint="Nhập phản hồi..."
        android:minLines="2"
        android:maxLines="4"
        android:inputType="textMultiLine"
        android:background="@android:color/transparent"
        android:layout_marginTop="4dp"
        android:textSize="14sp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/btnSaveReply"
        android:text="Lưu phản hồi"
        android:backgroundTint="#1976D2"
        android:textColor="@android:color/white"
        android:textSize="13sp"
        android:layout_gravity="end"
        android:layout_width="wrap_content"
        android:layout_height="36dp" />

</LinearLayout>
